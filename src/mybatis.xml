<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

	<!-- 通过别名简化对类的使用 -->
	<typeAliases>
		 <!-- 
			通过package, 可以直接指定package的名字， mybatis会自动扫描你指定包下面的javabean,
		           并且默认设置一个别名，默认的名字为非限定类名来作为它的别名。    
		  -->
	 	<package name="indi.test.entity"/>
	 	<package name="indi.scm.entity"/>
	 </typeAliases>


	<!-- 配置在applicationContext.xml中已经被简化  面向接口 -->
	<!-- <mappers>
		<mapper resource="indi/test/entity/DeptMapper.xml"/>
	</mappers> -->
	
</configuration>